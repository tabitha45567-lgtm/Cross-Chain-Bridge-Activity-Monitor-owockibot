import React from "react";
import ReactDOM from "react-dom/client";
import BridgeMonitor from "./BridgeMonitor.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BridgeMonitor />
  </React.StrictMode>
);
